# Recovered from the session an org spend limit killed, 2026-08-20

These two documents are the ONLY surviving record of the Today screen design
work, and they were recovered with about an hour to spare.

The session's scratchpad under `/private/tmp` was **already empty** when this was
written, and the four task transcripts that held the mockup's generator
(`build.py`, `mock.css` and the rest, quoted in full as heredocs) had been
truncated to 156 bytes each between one read and the next. The durable session
transcript under `~/.claude/projects/` does NOT contain sidechain agent content,
so nothing there covers it either.

`TODAY-FINDINGS-R1.md` and `TODAY-FINDINGS-R2.md` are R2 in particular: it holds
every measured decision for all four of Tom's Today notes. Read R2 first.

The larger transcripts that still had content are parked outside git at
`~/Documents/boneheadz-dead-session-transcripts` (203 MB, too large to commit).
They are a single copy on one disk. If any of the Today mockup is to be rebuilt
rather than redesigned, its generator has to come from there.

**The lesson, which is now the second time today: a scratch worktree or a task
output under /private/tmp is not storage. It is a file with a countdown on it.**
