# Train 542 integration advisory

Frozen plan SHA256: `cefcaf029f5e107a8c10f96932c085b91d13fd8461ad6321a8eafba8a478a556`. The supplied file matched. Starting checkout: `b48f2e2c`, clean. All source paths were resolved in this checkout. This is advisory evidence for independent review.

The implementation preserves the five assembled lanes. Production changes add an appearance-specific destruction quote field, disclose when the last ordinary Base appearance is lost while a shiny preserves its collection cell, and restore the required health-guard coverage call in the release gate. Existing Base-cell ownership, Breed stale-quote checks, typed investment consent, durable waiting counts, recovery invitations, incubator availability and offline purchase reconciliation remain in place.

| Audit | Cause and decision | Final evidence and regression proof |
| --- | --- | --- |
| `lab-room2-audit.mjs` | Real code gap at the r6-cells/r6-app boundary, exposed by r6-guards finding 15. The ordinary Base is correctly no longer the last collection copy, so destruction uses quick confirmation. Neither its toast nor the shared disclosure distinguished appearance from ownership. Added `lastAppearance` to the atomic quote and explained shiny preservation in the shared loss text. The grade is unchanged; removed only its obsolete expected-red label. | All 16 declared findings pass. Actual pre-integration `b48f2e2c` app source fails finding 15. Its existing synthetic negative control also remains rejected. |
| `breed-last-colour-audit.mjs` | Stale harness after r6-app introduced the rendered `breedQuote` and `labEqual` comparison. The harness had extracted only the click handler and threw `ReferenceError`. It now evaluates the production panel, quote and handler together. Updated the changed-stock scenario to assert the intended refuse, rerender, then review sequence, with no consumption and the new Midnight warning. | Named last-cell loss, wrong text, explicit consent, shiny keeper, changing stock and cancellation pass. Removing typed last-cell protection causes the unchanged two-tap consumption assertion to fail. |
| `stable-loss-disclosure-audit.mjs` | r6-guards correctly added Breed investment coverage, but the shared harness did not include r6-app's new quote closure. Fixed the harness, retained every investment assertion, and removed obsolete expected-red wording. | All six investment fields are present. The actual pre-app-lane source at `b48f2e2c^` fails with `missing=banked steps, nickname, bond, talents, talent name, lineage`. No R6-S3 assertion is waived. |
| `lab-ui-audit.mjs` | Stale expectation after r6-app R6-A1 intentionally kept recovery access visible for unknown experiments. Replaced the blanket unknown-state suppression assertion with positive recovery-link, status and hide assertions. Ordinary offer eligibility and suppression assertions remain. | 30 groups pass. Removing hide enforcement still fails the original gating assertions. Restoring unknown-state suppression fails the new recovery assertion. Recovery works on the first day, while current-tab and explicit-hide constraints remain enforced. |
| `kennel-copy-audit.mjs` | Stale DOM double after r6-app added `isConnected`: its button lacked that property, so the real handler returned before arming. Reused the shared connected-button and rendered-quote harness. The colour/level assertions are unchanged. | 16 groups pass. A reconstructed generic species-only spare name fails R44-14 with `armed breed button omitted colour/level`. |
| `stable-rooms-top-audit.mjs` | Stale fixture after r6-app R6-R1 moved the door from renderable `unseen` rows to durable `waiting` data. Fixtures now supply durable counts with empty render lists. Unavailable snapshots correctly require `Check status`. All room-order, icon, count, attention, colour and corner assertions remain. | 18 template cases pass. Moving the room below the album still fails the original order assertion. Reverting to render-list counting fails the live-count assertion. The separate r6-app guard executes the actual durable count helper, including deduplication and seen receipts. |
| `breed-two-tap-audit.mjs` | Stale shared harness after r6-app introduced the rendered quote. Fixed the harness; this audit file is unchanged. DOM dispatch still clears `currentTarget` immediately after dispatch. | Both taps pass: roster 4 to 3, keeper lineage 1, cooldown credit 0 written and result opened. Actual pre-event-fix app source at `0f860173^` fails on null `currentTarget.dataset`. |
| `collection-cell-audit.mjs` | Diagnosed after the seven. Its eight Breed reds were the same missing r6-app quote closure, not incorrect r6-cells ownership. Fixed the harness and strengthened the existing eight shiny-morph cases with explicit appearance/preserved-cell checks, including successful two-tap Breed. | 123 groups pass. Off-grid CX and ordinary duplicate controls reject false shiny-preservation copy. Actual pre-cells `js/pets.js` and `js/loot.js` from `b48f2e2c^` fail the real last-cell warning assertions. The independent hostile seven-cell census and eligibility guards remain. |
| `r6-guards-audit.mjs` | Diagnosed last. Two assembly gaps: the gate omitted the r6-guards `requireLabFindingCoverage(onDisk, PURE)` call, and the audit assumed pending notes remained in `vNEXT` after assembly moved them to v542. Restored the production coverage call. Grade the latest shipped notes plus any nonempty pending notes, keeping exact note and proof-row counts. Raised the minimum PURE census to 156. | Four control groups pass. Removing the gate call fails. Removing a shipped proof row fails at 14 versus 15. Existing missing-file, deregistration, 14/16 finding-denominator, scroll-selection and evidence-label controls remain. |

The shared Breed harness now reads the live roster during rerenders, selects the first two fixture pets and runs the production render closure before attaching the real listener. `r6-app-harness.mjs` reexports it, preventing the two previously divergent copies from drifting again. These remain Node DOM doubles, not browser interaction proof.

Validation commands, complete enumeration, source hashes and captured results are recorded in [proof.json](proof.json). The full per-process streams remain outside the checkout under `/tmp/train542-proof/`.

```text
node tests/unit.test.js
384 passed, 0 failed

node docs/train542-integration/run-pure.mjs /tmp/train542-proof/final-pure
159/159 PURE entries exit 0

node tests/release-gate.mjs --coverage-only
coverage: 414 audits on disk, 126 fast, 129 full, 159 skipped

node docs/train542-integration/prove-red.mjs /tmp/train542-proof/final-red
12/12 defect controls rejected
```

The coverage-only command's `159 skipped` is its tier-inventory wording. The separate PURE runner executes those 159 entries, each in a fresh Node process. It evaluates the actual `const PURE` section through `const BROWSER`, including every push/unshift, refuses fewer than 156 or duplicate entries, and honors the gate's serial-suite list. No audit was removed, reclassified or counted green without exit 0. `r6-app-audit.mjs` passes all six controls. Its actual pre-app-lane source also produces six separate RED results for S3, R1, C3, C2, R2 and A1.

The red-proof runner uses disposable copies. It labels actual historical-source replays separately from reconstructed defect mutations. Each child must exit 1 and reach the expected defect assertion; timeouts or unrelated errors do not count as regression evidence. For the async-event regression, the expected failure is the original null-event-target exception. The initial proof runner exceeded Node's default source-output buffer; its buffer was increased and all controls reran successfully.

No version stamp or changelog was changed. All existing v542 CLAIMS rows remain byte-for-byte intact. An additive table row records the corrected appearance disclosure, the now-passing investment disclosure and the restored finding-coverage enforcement.

Denied or blocked actions: none. No commit, push or publication was attempted. No original checkout was edited. Deviations from the frozen work order: none. Browser rendering, layout and deployment acceptance were not run and are not claimed by this Node-only proof.

Files changed:

- `js/app.js`: shared appearance-preservation loss copy, also included in the quick Breed toast.
- `js/loot.js`: atomic `lastAppearance` quote field, restricted to collection pets.
- `tests/release-gate.mjs`: invoke required Laboratory finding coverage.
- `tests/lib/pet-destruction-harness.mjs`: execute live production Breed rendering and listener closure.
- `tests/lib/r6-app-harness.mjs`: reexport the shared harness.
- `tests/breed-last-colour-audit.mjs`: assert the current stale-review sequence.
- `tests/collection-cell-audit.mjs`: stronger appearance/preserved-cell checks for every shiny fixture.
- `tests/kennel-copy-audit.mjs`: use the real connected-button/render harness.
- `tests/lab-ui-audit.mjs`: recovery visibility and hide controls.
- `tests/stable-rooms-top-audit.mjs`: durable waiting-count fixtures and honest status copy.
- `tests/r6-guards-audit.mjs`: assembled and pending claim coverage, minimum 156 census and missing-proof control.
- `tests/lab-room2-audit.mjs`: remove obsolete expected-red wording, retain grades.
- `tests/stable-loss-disclosure-audit.mjs`: remove obsolete expected-red wording, retain loss assertions.
- `docs/CLAIMS.md`: additive v542 correction row.
- `docs/train542-integration/run-pure.mjs`: reproducible complete PURE runner.
- `docs/train542-integration/prove-red.mjs`: reproducible historical and mutation controls.
- `docs/train542-integration/proof.json`: machine-readable enumeration, statuses and hashes.
- `docs/train542-integration/REPORT.md`: this advisory.
