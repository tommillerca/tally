# Everything from 2026-08-21, and where each thing stands

Written because Tom left mid-session and asked that nothing live only in my head.
`origin/main` is `aded834d` (v420). **No version stamp has moved all day**, so
none of this has reached an installed phone.

## LANDED

- **PR #79 merged.** The gate had been exiting 1 on every branch, before a single
  test ran, because `tests/boneyard-icon-audit.mjs` belonged to no tier. Four
  separate agents hit it independently. Merging it also landed the purchase P0,
  the rack theme lint and the weekly reroll. Verified after: the gate now starts,
  `175 audits on disk, 50 fast, 95 full, 30 skipped`.

## BRANCHES PUSHED, NONE MERGED

| branch | tip | state |
|---|---|---|
| `ext/crew-docs-rescue` | `34d2e2d8` | 7 crew docs that existed on no ref |
| `ext/recovered-today-findings` | `8ea02397` | the Today design record, recovered hours before /private/tmp ate it |
| `ext/emporium-landable` | `08aa59b2` | Gwart's Emporium, centring fixed, stamp stripped. **GAP: its guard does not exist** |
| `ext/bumbleseal-pets` | `65eca6e9` | pets, slots, 8-row lint, unit suite 186/0. **GAP: not purchasable** |
| `ext/mystery-egg-one-name` | `cff5f458` | one name from one constant |
| `ext/mimic-wanderer` | `3f3a232d` | both new bosses in the Gauntlet |
| `ext/wanderer-patrol` | `5f29fc1a` | **supersedes `ext/wanderer-boneyard`**, which built the wrong creature |
| `ext/today-hype-banner` | `1784d1e4` | one banner, ten words, replaces the four-banner stack |
| `ext/route-flash` | `2601f8db` | the tray flash, 108ms to 0 |
| `ext/mockup-motion-truth` | `765474b9` | the motion guard, both states |
| `gwart/v420-dbprune` | PR #77 | needs migrations AND a cron trigger, not just a deploy |
| `gwart/v420-writefail` | PR #78 | `js/db.js` is precached, so it ships only when VERSION moves |
| `rescue/wt/*` | 21 snapshots | **mine, do not merge**. One of them is a deliberate bug |

## WAITING ON TOM

1. **Reduce Motion.** Settings > Accessibility > Motion. Measured: every banner
   runs 29 to 50% changed pixels with motion allowed and **exactly 0.000%** under
   reduce, and the drop reel's JS driver is skipped entirely so it rests on its
   first beat forever. That is the "static grid" he has always seen. One look
   confirms it. Also worth asking about Low Power Mode.
2. **The Wanderer's Step Egg.** He currently drops it on a win. One line to
   remove. The argument for keeping it: a pure hazard has one correct play,
   which is to walk around behind him forever.
3. **Pet rarities and names.** Prices are his (50,000, and 3,500 to 12,000).
   Rarities and names are still mine and they set salvage value.
4. **The herb icon.** Two PixelLab links were sent; the PNGs are not on disk.
   Note `rescue/wt/cur` already carries an unclaimed `assets/icons-pix/food/herb-sprigs.png`.
5. **The Today hero at 620.** Costs nothing and reveals 64px of the next card;
   at today's 688 the card misses the fold by 4px so nothing peeks at all.
6. **The R3 Today mockup's motion.** It carries `gwFloat`, `zEnterCine`,
   `zOrbitSoft` and `zTw*` that exist NOWHERE in the app. Either port them or
   mark the mockup unbuilt, before someone reports it as shipped.

## STILL MINE TO FINISH

- Tab bar Poster Stickers, written into app.css, needs committing and a guard.
- Today: currencies above Gwart, and the scroll peek. Asked for three times.
- `tests/emporium-audit.mjs`. `app.css` on that branch says "Guard:
  tests/emporium-audit.mjs" and **that file exists on no ref**. The comment is
  currently a lie in shipped-looking code.
- Wiring Bumbleseal into the shop. The hero banner and accessory shelf he
  approved are a mockup, not code.
- Porting the mockup's Today changes into the app: plate `#d5c8b0`, the gradient
  starting at 70% instead of 38%, the bonehead shifted 58px left, the pet at 169px.

## IN FLIGHT

Three agents, all relaunched after dying on connection resets, now told to push
as they go: the overscroll wordmark truth-check, the pixel-art swap sweep, and
the Kickstarter banner.

## TWO THINGS I GOT WRONG TODAY, RECORDED SO THEY ARE NOT REPEATED

1. **I reported branches green having run only some of their audits.**
   `ext/mimic-wanderer` was red on `reward-sop` and `fight-exit`; a Mimic win said
   "Back to The Pit" from the Boneyard. `ext/bumbleseal-pets` was 184/2 on
   `npm test`. Both were caught by other agents, not by me. Run the suite, not a
   selection from it.
2. **I measured DOM boxes and called them ink.** I reported the Emporium art
   "spilling" 481px into a 393px viewport and the hero eating 57% of the screen.
   Both numbers were the `<img>`, which is a mostly transparent 2048 square. The
   visible ink is 379px and clipped at neither edge. Measure the pixels.
