# Off-hand Round 5 advisory

The final requested offsets are applied relative to the preserved original masters: spades IL17-1/2 (+24,+32), brushes IL10-1/2 (+22,+86). This checkout started with Round 4 already applied. Adding the new offsets to HEAD would have compounded that rejected adjustment, so the hash-pinned originals under `docs/offhand-registration/before/` are the source. No original checkout was edited.

Frozen plan SHA256 verified: `328119896a5122ce706585d52ba2a75f9242a26b63676ff870e2fc4381a2f004`.

## Pixel proof and deviations

The six scratch slots were restored before any other mutation. Each `git hash-object` matched its HEAD blob. They were already clean at entry and have no final diff. [Printed hashes](round5-restored-hashes.txt).

All loss preflights printed before writing any master. Integer, unmasked RGBA copies performed no redraw, recolouring, blending, rescaling or resampling. Reopened PNGs match the expected translated bytes, and each surviving visible RGBA multiset is unchanged. [Printed assertions](round5-pixel-proof.txt), [structured proof including every lost pixel](round5-pixel-proof.json), [read-only recheck](round5-final-integrity.txt), [reproducible verifier](round5-verify-translation.py).

| Master | Offset from original | Lost alpha >0 | Lost alpha >30 | Source face coverage |
| --- | --- | --- | --- | --- |
| IL17-1 | (+24,+32) | 7 | 0 | 0.00% |
| IL17-2 | (+24,+32) | 10 | 0 | 0.00% |
| IL10-1 | (+22,+86) | 0 | 0 | 1.62% |
| IL10-2 | (+22,+86) | 0 | 0 | 1.61% |

Round 5's zero-pixel-loss requirement is impossible for these original spades and exact offsets. Their export dust extends to x631/x639; shifting by +24 places those pixels outside the 640 frame. This contradiction was reported before application. The proposed deviation used here preserves the exact final offsets and invokes the work order's explicit allowance to discard export dust at alpha <=30. Maximum lost alpha is 11 for IL17-1 and 30 for IL17-2. Nothing above alpha 30 is lost. Independent review must accept this deviation before assembly; zero-loss acceptance is not claimed.

The complete original alpha>0 bounding boxes also cannot move by exactly the offset because their rightmost dust is clipped. The surviving alpha>0 boxes do move exactly; the complete alpha>30 boxes move exactly without clipping:

| Master | Original alpha >30 box | Final alpha >30 box |
| --- | --- | --- |
| IL17-1 | (364,40,543,358) | (388,72,567,390) |
| IL17-2 | (364,40,542,357) | (388,72,566,389) |
| IL10-1 | (346,1,602,363) | (368,87,624,449) |
| IL10-2 | (346,1,601,362) | (368,87,623,448) |

The operator supplied 1.64% face coverage for both brushes. The unchanged local facegate mask gives 1.62% and 1.61% for these exact translated originals. Both pass the unchanged 2% limit; no offset or threshold was altered to match the supplied table.

The project's `scripts/build-bh-thumbs.py` module generated each affected master through its `wanted()` function with `optimize=True`, covering 192, 384 and trim. All eight square thumbnails and IL17-2's trim changed. Three other trims regenerated byte-identically. IL17-2's trim changes because the cropped export dust included alpha 17 to 30, above the generator's alpha>14 trim threshold. [Generator output](round5-thumbnail-build.txt).

## Render evidence and blocked actions

The frozen Round 5 instruction reports that the operator searched under the facegate constraint and selected the final offsets in the shipping avatar at 393x852 DPR 2 against the correctly held IL7-1 Blue Katana. That is attributed operator evidence. The four earlier rendered search rounds selected a visually approved candidate that facegate rejected at 28.53% brush coverage. The vNEXT row records both facts and matches the single NEXT_CHANGES item. No version stamp changed.

This sandbox's capture attempt exited 1 before rendering: `listen EPERM: operation not permitted 127.0.0.1`. [Full output](round5-render-attempt.txt). No listener-denial bypass was attempted. The existing screenshots were preserved; no historical after image is relabelled as final proof. New before/after pairs and numeric rendered gaps are unavailable. A local render cannot independently confirm the operator's acceptance here.

Operator command, from this checkout:

```sh
HEADLESS_MODE=shell node /tmp/offhand-fix-real-render.mjs "$PWD" after
```

No commit, push, publish or deployment was attempted. Off-limits files `js/app.js`, `js/pets.js`, `js/loot.js`, `js/db.js` and `js/social.js` remain unchanged, as do the control sprite, the source diagnostic and release-gate registration. There were no approval requests or approval-review rejections.

## Proof results

- `node tests/unit.test.js`: exit 0, **384 passed, 0 failed**. [Output](round5-unit-output.txt).
- `node tests/facegate-audit.mjs`: exit 0, **62 held items checked**, worst coverage 1.82% across the whole held-item set, below 2%. [Output](round5-facegate-output.txt). The per-changed-file measurements are above.
- `node tests/release-gate.mjs --coverage-only`: exit 0, `coverage: 415 audits on disk, 127 fast, 129 full, 159 skipped`. [Output](round5-coverage.txt).
- Every PURE entry was enumerated from the declaration and push/unshift registrations in `tests/release-gate.mjs`: **159 unique entries, 159/159 exit 0**, above the required 156 minimum. No omission or timeout. The unit entry reuses the completed agreed command's result; every other audit ran in its own Node process, respecting SERIAL registrations. The thumbnail freshness audit, including its controls, passed. [Driver](round5-run-pure.mjs), [enumeration and exits](round5-pure-results.txt), [structured results](round5-pure-summary.json), [full output](round5-pure-output.txt). This result covers the PURE tier only; the browser release gate was not run.
- `git diff --check`: exit 0.
- The unchanged source hand-registration diagnostic exited 0 and printed 0 historical failures. [Informational output](round5-diagnostic.txt). It is not alignment evidence and is not cited by the vNEXT PROOF row. Relative to the previous recorded candidate, brush core/ring scores change from 1292/322 and 1288/321 to 36/691 and 37/694; spades change from 832/230 and 825/230 to 104/635 and 119/630. These scores do not establish rendered correctness.

## Files changed

- `assets/bh/IL/IL10-1.png`, `IL10-2.png`, `IL17-1.png`, `IL17-2.png`.
- The same four filenames under both `assets/bh/thumb/192/IL/` and `assets/bh/thumb/384/IL/`.
- `assets/bh/thumb/trim/IL/IL17-2.png`.
- `docs/CLAIMS.md` and `js/changelog.js`.
- New evidence under `docs/offhand/`: `round5-report.md`, `round5-verify-translation.py`, `round5-pixel-proof.txt`, `round5-pixel-proof.json`, `round5-final-integrity.txt`, `round5-restored-hashes.txt`, `round5-thumbnail-build.txt`, `round5-unit-output.txt`, `round5-facegate-output.txt`, `round5-coverage.txt`, `round5-render-attempt.txt`, `round5-diagnostic.txt`, `round5-run-pure.mjs`, `round5-pure-results.txt`, `round5-pure-summary.json` and `round5-pure-output.txt`.

This report is advisory for another provider's independent review. Exact requested offsets are preserved, but the zero-loss and fresh-render evidence limitations above remain explicit.
