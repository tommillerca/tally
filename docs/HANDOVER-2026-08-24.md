# Handover, 2026-08-24

For the team taking this over next week. Read `ONBOARDING.md` first: it is the
entry point and it stays the entry point. This file is only what a new team needs
that is not already written down, plus the state of the work in flight.

**Live:** https://tommillerca.github.io/tally/ &middot; currently `tally-v431`
**Owner:** Tom Miller. He plays the live build daily and reports from it.
**Repo:** `tommillerca/tally`, and it is PUBLIC. See "Two remotes" below before
you push anything.

---

## 1. The one habit that matters here

Every expensive mistake in this project's recent history is the same shape: a
check that reported success **without examining the thing that was broken**.

Recent, real, and each one cost a day or a false claim to a player:

| what looked fine | what was actually true |
|---|---|
| An audit passing 15/15 on the friend paddock | Every row opened the sheet through a test hook. Deleting the real tap handler left all fifteen green |
| A memory suite reporting a ceiling breach | It was clicking a class that had not existed for months, with `?.` swallowing the miss, and grading a screen it never opened |
| Three lints reporting clean | The file they were grading could not be parsed by Node at all |
| A wordmark guard counting "ink pixels" | Olive art clears the brightness threshold exactly as the mark's cream does, so it counted art and concealed a real conflict |
| A patch note saying a feature shipped | It was behind a query flag no player has |

So: **verify the end of the chain, not the step you performed.** A green merge
says the code is on main. It says nothing about whether a player can reach it.

## 2. What runs, and what it costs

```
node tests/release-gate.mjs --as=<yourname>     # fast tier, minutes
node tests/release-gate.mjs --all --as=<name>   # everything, ~25 min
```

230 test files, 146 registered in the gate. Three tiers: `PURE` (node only,
instant), `BROWSER` (fast), and `DECLARED` full runs.

Two things that will bite on day one:

- **An audit on disk that the gate does not declare makes the gate exit 1 before
  a single test runs.** If the gate dies immediately, that is why.
- **`--as=<name>` claims a lock** in `CHAT-HANDOFF.md` above the repo. Two
  concurrent gate runs on one machine poison each other's timings. Contention
  biases a suite toward TIMING OUT, so a RED taken under load certifies nothing
  while a GREEN under load is credible.

`HEADLESS_MODE=shell` is forced on the owner's Mac and is itself a source of
intermittent unpainted frames. A blank-looking capture probably IS blank: reject
and retry rather than measuring it. Map suites additionally need
`--use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader`.

## 3. Four guards that hold the process, not the code

These are recent and deliberately mechanical, because rules get forgotten and
lints do not. Do not delete them to go faster.

- **`feedback-status-lint`** every item Tom has ever asked for carries
  OPEN / SHIPPED / DROPPED, and the open ones PRINT on every gate run.
- **`claim-evidence-lint`** every item in the newest changelog entry must be
  answered in `docs/CLAIMS.md` with PROOF (an audit that exists and the gate
  runs) and REACH (how a player gets there). `GATED`, `PENDING-DEPLOY` and
  `NEEDS-PEER` are honest answers and any of them FAILS a shipped entry.
  This exists because four patch notes in one day told players about things they
  could not see.
- **`guard-hygiene-lint`** carries three ratchets: audits with no positive
  control, audits Node cannot parse, and audits that only ever touch a test hook.
- **`guard-provenance-lint`** a pinned expectation constant must cite a dated
  source. A number with no method behind it is not evidence.

## 4. Two remotes, and one of them is public

`origin` is **tommillerca/tally, PUBLIC**. `private` is
**tommillerca/tally-source**.

Monetization scoping, the shop economics, the work register and the cross-session
handoff live on the PRIVATE remote and must stay there. A branch's upstream is
not always the one you assume: a work register spent seven hours on the public
repo this month because a bare `git push` from the wrong clone published it.

**`git push --dry-run` is the only command that answers "where would this
actually go".** Run it before you believe anything is safe.

Do not force-push. Two agents rewrote public history in one day this week;
nothing was lost, but nothing sanctioned it either.

## 5. Measuring what a branch actually contains

Three-dot diffs and `git merge-tree` both lie, and both lied to me this week.

- `git diff main...branch` diffs from the MERGE BASE, so work already shipped
  under another PR still looks new.
- `git merge-tree` on a conflicting branch returns a tree full of conflict
  markers, so diffing it counts `<<<<<<<` as content.

The honest answer is a real merge in a throwaway worktree, then
`git diff --cached --stat`. A branch that "adds 420 lines" was, measured properly,
a stale draft that would have reverted a shipped fix.

## 6. Prove-red discipline, and two traps in it

Every guard row must be shown to FAIL on the real defect before you trust it.

1. **`cp -R` of a git WORKTREE is not isolated.** The copy's `.git` is a file
   pointing back at the original, so `git checkout <ref> -- path` inside it writes
   somewhere else and your prove-red passes while proving nothing. Build throwaway
   trees with `tar` excluding `.git`, mutate the FILE directly, and grep the copy
   to confirm the mutation landed.
2. **If your revert also removes a test SEAM the audit needs**, the audit fails
   for the wrong reason while its real rows pass green. Isolate the defect from
   the harness: disable only the fix, keep the seam.

## 7. State of the work

Updated 2026-08-25. Shipped this week: **v427 through v438**. The friend-add
button (which was impossible, not slow), the app-shell rework (13,809ms to 78ms
on a bad connection), the Wanderer's water oracle, Gwart's Guide, the Today
ledger, the transmog rework, the island bleed, a friend's paddock you can walk
into and stand in, the Wardrobe's cropped art tier, and four builds' worth of
fighting with one CSS surface (see below, it is the most useful thing in this
file).

**Closed since the first draft of this section:**

- **The bots are gone, not flagged.** 47 test accounts deleted 2026-08-25 after
  verifying against the LIVE database rather than the census doc: all 47 at level
  1, **zero friendships** (so no real player lost a connection), zero recovery
  codes, spires, trades or fights, every handle an auto-generated default that
  never had a display name, all created in tight CI-time clusters. Player count
  **98 to 51**, friendships still 80, the 14 real progressed players untouched.
  Four migrations were applied to production D1 in order; `SERVER-DEPLOY-PENDING.md`
  was STALE when I read it (it claimed none had ever been applied; the 2026-08-16
  hardening one already had). **Derive schema truth from the live database.**
- **`memory-census` OFF-DOM is green.** Was 93.8-101.6 MB against a 90 MB bound.
  The Wardrobe's cropped tier took it to **11.6 MB**. The bound was not widened,
  which was the instruction in the previous draft and it was the right one.

**Open, and each needs Tom rather than code:**

1. **The Worker deploy.** Still not run: `server/deploy.sh` was refused by a
   permission gate, correctly. `crons = []` is already committed so deploying
   starts NO pruner. It is now much lower stakes than this section used to say,
   because the bots were deleted rather than hidden, so the leaderboard is
   already clean without it. What the deploy buys is the `is_test` filter for
   FUTURE test accounts.
2. **#77's pruner and its 15-minute cron.** The genuinely irreversible one:
   deletes rows from a live database on a schedule and has never run against real
   data. Deliberately left off. Wants someone watching the first tick.
3. **The cauldron icon.** Candidates rendered on `art/kitchen-cauldron-v2`.
4. **The Bonehead's off-centre shift** (`--bh-shift`), and **the walk potion**
   (`docs/DESIGN-walk-potion.md`, which argues against building it).
5. **Two notes from 2026-08-25, logged in ROADMAP.md, investigated, NOT built:**
   the residual seam between the pull-down fill and the art (+3/+2/+4, measured,
   three options written up) and the wordmark needing to clear BEFORE the UI
   returns under it. Read the ROADMAP entry before touching either; both have a
   guard that will bite.

**Two audits are RED on main, and neither is a player-visible bug:**

- `nav-perf-audit` SAMPLE. The cold lap must scan >=400,000 px in **>=6** calls
  to prove the detector works. It now scans 409,600 px in **1**, because the app
  legitimately does less alpha-scanning since the cropped tier landed. Verified
  it fails identically on v435 and v436, so it is not one change's fault, and it
  passed earlier the same day at 587,391 px in 8 calls. The PREMISE is stale, not
  the app. Re-anchor it on "cold scanned strictly more than warm" rather than a
  raw floor, and prove it red by breaking the probe.
- `badge-centre-audit` COVERAGE. The required disc (the bolt in the Boneyard
  readout, `span.ic.fast`) IS found and measured at an ink offset of
  (-0.21, -1.53)px, i.e. essentially perfectly centred. The audit refuses to
  GRADE it only because its rect intersects a neighbouring disc and the method
  cannot weigh overlapping discs. Either teach it to, or decide two badges should
  not overlap there. Do not just delete the coverage requirement.

**One open PR that is not mine:** #158, another session's work taking the Stable,
Paddock and Shop off 240 MB of pet art. Not reviewed here.

## 8. Where the rest is written

`ONBOARDING.md` the rules and the why &middot; `ARCHITECTURE.md` &middot;
`ROADMAP.md` decisions taken and held &middot; `docs/SHIP-LEDGER.md` what counts
as live &middot; `docs/STATE-2026-08-23.md` the fuller state &middot;
`docs/GOD-MODE.md` the test harness &middot; `docs/CLAIMS.md` the claim ledger
&middot; `TESTFLIGHT.md` and `docs/` for the native builds.

## 9. Scope, access and who decides

Settled with Tom, 2026-08-24.

**You get everything.** Both remotes, including the private one. Nothing is
fenced off, so read `private/`'s monetization scoping and shop economics before
you touch anything priced: the balance is HELD and the cosmetic-only rule
(2026-08-07, flair never power) is a decision, not a preference.

**Credentials:** Tom's read is that you already hold what you need. If anything is
missing, Cloudflare and D1, the Apple developer account and TestFlight, and the
PixelLab account the pixel art comes from are all his to grant. None of it is in
this repo and none of it should be.

**Decisions stay with Tom, and he wants to be asked.** His words: "I can handle
them if you can tell them to ask me what they need to continue." So do not guess
at a taste call or an economy change to keep moving. Ask him. The five open items
in section 7 are all his, and the two that will bite soonest are the D1 deploy
(players are still seeing test accounts) and the memory overage (do not widen the
bound).

He plays the live build daily and reports from it, so he is the fastest bug
reporter on the project and the only one whose reports come from a real device.

## 10. The surface that cost four releases, and the harness that ends it

Read this before you touch Today's scroller. It is the clearest example in the
repo of the habit in section 1, because it is four consecutive failures of it by
the same author in one day.

**The surface.** `.screen--today` has to do two incompatible-looking things: the
strip an iOS rubber-band pull opens at the top must show the Bonehead's backdrop
colour (the wordmark prints on it), and the page below must show the app's own
backdrop and never that colour.

**What actually paints the strip, measured on a booted iPhone 17 Pro:** the
scroller's `background-color` and NOTHING else. Not a `::before` at
`bottom: 100%`, not a real div there, not a `background-image` at
`attachment: local` or `scroll`. All four scored ZERO pixels. WebKit sizes the
scrolled-contents layer to the scrollable-overflow rect, which begins AT the
scroll origin, and fills the rubber band from that layer's solid backgroundColor.
Anything above the origin is not in the layer that gets translated.

**The four failures, in order:**

| build | what shipped | what Tom saw |
|---|---|---|
| v434 | `background-color` only | bounce right, colour bled through every gap |
| v435 | + an opaque `background-image` | "no colour background above by the wordmark", page went flat black |
| v436 | no background, a `::before` strip above the origin | still no colour, and the wordmark lingered on release |
| v438 | `background-color` only again, page covered from INSIDE the content | correct |

**The answer was already written in `app.css` before v435 was even started.** The
device measurement above sits at the top of that file with a date on it. Nobody
read it. Three builds went out rediscovering it badly, and the fourth is just
v434's configuration with the page problem solved somewhere else.

**The mechanical lesson**, for whoever meets this next: `background-repeat`
cannot rescue a background-image here. A gradient auto-sizes to the whole
background positioning area, so `repeat` and `no-repeat` render identically. The
presence of an opaque image is the thing that breaks it, because the compositor
no longer has a simple solid background to promote onto the layer.

**How the page gets covered instead:** `.today-plate`, a zero-height sticky FIRST
child at `z-index: -1`, painting the app's backdrop behind Today's content. Three
details in it are each a bug that was hit and fixed, so do not simplify them
away: it must be the first child (anything above the origin cannot help), it must
have zero height (a real height gave it margins that COLLAPSED with the hero's
negative top margin and shifted every card down 69px), and it needs negative side
margins (`.screen`'s `var(--pad)` otherwise leaves a strip of backdrop colour
down both edges).

**And the harness, so this is a two-minute question and not a four-release one:**
`tests/manual/bounce-probe/`. A bare page with the same `#app`/`.screen` boxes and
`--hero-edge` forced to magenta. Serve it, open it on a booted simulator, hold a
drag and screenshot mid-gesture. Its README has the commands.

**Do not try to measure this inside the real app.** A drag in MobileSafari fires
Safari's OWN pull-to-refresh instead of the inner scroller's bounce, so the page
reloads and you measure nothing; `overscroll-behavior: contain` is already on
`.screen` and does not prevent it. The service worker will also happily serve you
cached CSS over the change you are testing. Both of those produced a full round
of confident, meaningless readings on 2026-08-25 before I noticed.

