# v475: the rack holds still, and the Crew gets honest

## Summary

- Locker Room polish from live feedback: Today's News pill now announces the
  room the moment it opens and one-shots straight into the Shop
  (news-banner-audit.mjs, news-tab-audit.mjs, shop-lead-order-audit.mjs); every
  tile shows its own 32-team colour strip and the full-kit tile shows all five
  garments in five different teams instead of one two-tone disc
  (shop-lead-order-audit.mjs TINT-STRIP/BUNDLE-5, art-resolution-audit.mjs,
  memory-census.mjs); and the two lizard tiles draw from the same 384px tier
  the poster hero already uses instead of a blurry 192px source
  (shop-lead-order-audit.mjs PET-384, football-render-audit.mjs).
- Rack: the rotating twelve now turn over once a week on the same Monday
  boundary as the rest of the shelf instead of silently re-rolling nightly,
  the scarcity clock moved into Gwart's own header, and the week's free
  reroll sits beside the shelf instead of far below it
  (rack-rotate-audit.mjs, purchase-firewall.mjs, shop-lead-order-audit.mjs,
  emporium-audit.mjs, t3-audit.mjs).
- Shop front door: the Bonehead tab reverts to always landing on the
  Wardrobe (a brief hub-tab-memory experiment collided with the standing
  v421 ruling and is re-premised back); the coin balance routes to Today;
  a coins-only Crew gift's "Spend it in the Shop" line is a real tap target;
  and the afford counter stops counting owned pieces against the wallet
  (shop-door-audit.mjs: HUB-HOME, WALLET, GIFT, COUNT).
- Crew identity and rank: onboarding name reaches the Crew instead of a
  random handle, race rank below 10th is the server's real number, a
  clock more than five minutes off is named as clock trouble instead of
  blamed on the server, the champion line shows on every request after
  settlement instead of only the one that settled it (server-side, see
  Deploy below), and long names get a tooltip instead of truncating or
  reversing silently (unit.test.js, crew-fan-audit.mjs, crew-pair-audit.mjs).
- Crew tab layout: an unopened gift now sits in its own card above the fold,
  the WORTH ADDING card collapses to one row with a See more control, a
  friend's side card in the fan is tappable instead of shadowed by the
  featured card, and a cheer with an apostrophe renders correctly
  (crew-layout-audit.mjs: GIFT, WORTH, HIT, TOAST).
- Offline crash seams: a rack buy, yesterday's day-close crate, and any
  quest claim now spend and grant in one atomic step, so a crash or a
  rejected write between paying and receiving can no longer half-charge or
  double-pay; a failed friends/profile sync no longer starts its five-minute
  retry cooldown (purchase-firewall.mjs, reward-sop-audit.mjs, unit.test.js).
- Also merged, not player-facing: `fix/sw-upgrade-audit-v473` re-premised
  four rows of sw-upgrade-audit.mjs that still graded the pre-v473 waiting
  policy superseded by v473's `letItIn()`; no app behaviour changed.
- ~152 gate-registered guards run in `tests/release-gate.mjs` (42 in the
  PURE tier, which runs on every gate); `node tests/unit.test.js` is 300/0
  on this tree.

## What CLAIMS.md backs

`docs/CLAIMS.md` carries a `## v475` section, one row per changelog item
(11 rows), each naming the audit that grades it and how a player actually
reaches it. `node tests/claim-evidence-lint.mjs` is clean: 11 of 11 answered,
no PROOF names an unregistered or missing audit, no REACH is GATED,
PENDING-DEPLOY or NEEDS-PEER for anything shipped as reachable today.

Three rows that did not make this changelog stay documented, unpromoted, in
their own dated sections below v475: a GATED Today teaser banner nothing
currently renders (`## shop front door`), an honest-403 gift-copy fix on a
branch the browser gate does not drive (`## crew layout`), and a welcome-kit
double-boot dedupe (`## claim hygiene`). The sw-upgrade-audit re-premise is
documented in its own `## sw upgrade audit` section since it changed no
player behaviour.

## Deploy steps (Tom)

Row 8 (the champion-line fix) is server-only: `server/src/index.js` now
back-fills `champion` from the settlement grants row on every `/steps/week`
read of the current week, not only the one response that ran settlement.
`git diff origin/main..integ/day3 -- server/migrations` is empty, so no
migration is needed, just the deploy:

```
cd "/Users/tommiller/Documents/Hyperframes Editor/tally/server"
git pull
npx wrangler deploy
```

## Verify live

After the web deploy (GitHub Pages):

```
curl -s https://tommillerca.github.io/tally/version.json
curl -s https://tommillerca.github.io/tally/sw.js | grep -m1 VERSION
```

Both must read `tally-v475` (they must agree, per `tests/version-align-lint.mjs`,
which is clean on this tree). Confirm the Worker deploy separately: opening
Crew's step race after settlement should show last week's champion on every
load, not only the first one after rollover.

## Test plan

- [x] `node tests/unit.test.js`: 300 passed, 0 failed
- [x] `node tests/version-align-lint.mjs`: web trio aligned at v475
- [x] `node tests/claim-evidence-lint.mjs`: 11 of 11 v475 claims answered
- [ ] Full `tests/release-gate.mjs` (not run in this worktree; the integrator
      runs the gate before merge, per the release process)
- [ ] Tom: `npx wrangler deploy` above (no migration pending)
- [ ] Tom: verify `version.json` and `sw.js` both read `tally-v475` on the
      live URL after the Pages deploy

🤖 Generated with [Claude Code](https://claude.com/claude-code)
