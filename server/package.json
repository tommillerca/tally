{
  "name": "bonez-api",
  "private": true,
  "type": "module",
  "scripts": {
    "db:local": "wrangler d1 execute bonez --local --file=schema.sql",
    "//dev": "--test-scheduled exposes /__scheduled locally, which is the only way to drive the retention cron in a test. Local dev only; it is not a route on the deployed worker.",
    "dev": "wrangler dev --local --port 8788 --test-scheduled --var DEV:1 --var ADMIN_TOKEN:devtoken",
    "//test": "schema-plan needs nothing running; the other two need `npm run dev`.",
    "test": "node schema-plan.test.mjs && node test/api.test.mjs && node retention.test.mjs && node grants-retention.test.mjs",
    "test:plan": "node schema-plan.test.mjs",
    "test:retention": "node retention.test.mjs",
    "test:grants": "node grants-retention.test.mjs",
    "deploy": "./deploy.sh"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
